# FastAPI URL Shortener

## Develop

### Setup

Right click 'delivery' -> Mark Directory as -> Sources Root

### Install packages:

```shell
uv install
```

### Run

Go to workdir:

```shell
cd delivery
```

### Run dev server:

```shell
fastapi dev
```
